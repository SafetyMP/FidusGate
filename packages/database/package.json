{
  "name": "@fidusgate/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx prisma generate && tsc",
    "dev": "tsc -w",
    "test": "npm run build && node --test dist/production-persistence.test.js"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "@fidusgate/core-types": "1.0.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.4.5"
  }
}
