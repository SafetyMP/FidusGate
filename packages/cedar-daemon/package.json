{
  "name": "@veritas/cedar-daemon",
  "version": "1.0.0",
  "private": true,
  "description": "Integration stub for high-speed Rust-native Cedar daemon",
  "scripts": {
    "build": "echo '📡 Rust compile task deferred (Rust compiler required for native performance, falling back to gateway shadow evaluation)'",
    "test": "echo '📡 Rust tests bypassed'",
    "lint": "echo '📡 Rust linter bypassed'"
  }
}
